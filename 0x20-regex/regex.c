#include "regex.h"

/**
* regex_match - determines if matches regex pattern in string
* @str: string to find patter in, any ascii other than . or *
* @pattern: Pattern to search in string (. or *)
* Return: 1 if pattern matches, else 0
*/

int regex_match(char const *str, char const *pattern)
{
	int i;

	(void)i;
	(void)str;
	(void)pattern;
	return (1);
}
